# 002 — Tasks

- [x] **TASK-1** Configure git identity before creating the release tag — file: `.github/workflows/release.yml` — implements REQ-1/AC-1
  - **TEST-1**: `tests/workflow/test_release_workflow_git_identity.py::test_create_git_tag_step_configures_identity_before_tagging`
