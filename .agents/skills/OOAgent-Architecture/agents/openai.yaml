interface:
  display_name: "OOAgent Architecture"
  short_description: "Repo-specific patterns and workflows for OOAgent-Architecture"
  default_prompt: "Use the OOAgent-Architecture repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true